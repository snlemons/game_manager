{
  "_comment_path": "Permissions baked for campaign at {{CAMPAIGN_PATH}}.",
  "_comment_recovery": "This file is gitignored and machine-local — clones don't carry it; /ingest Phase 1 regenerates it on a fresh clone. If $(pwd) doesn't match the path above, this campaign directory was moved on this machine. Re-run /ingest Phase 1 scaffolder against this directory, or update the paths in permissions.allow / permissions.deny to match $(pwd). The skills' settings-path preflight (references/preflight.md) detects this same-machine move case automatically; runtime detect-and-warn for moves is tracked in issue #21.",
  "permissions": {
    "allow": [
      "Edit(/{{CAMPAIGN_PATH}}/npcs/**)",
      "Edit(/{{CAMPAIGN_PATH}}/locations/**)",
      "Edit(/{{CAMPAIGN_PATH}}/factions/**)",
      "Edit(/{{CAMPAIGN_PATH}}/items/**)",
      "Edit(/{{CAMPAIGN_PATH}}/threads/**)",
      "Edit(/{{CAMPAIGN_PATH}}/consequences/**)",
      "Edit(/{{CAMPAIGN_PATH}}/beats/**)",
      "Edit(/{{CAMPAIGN_PATH}}/adventures/**)",
      "Edit(/{{CAMPAIGN_PATH}}/sessions/**)",
      "Edit(/{{CAMPAIGN_PATH}}/secrets/**)",
      "Edit(/{{CAMPAIGN_PATH}}/.ttrpg-staging/**)",
      "Edit(/{{CAMPAIGN_PATH}}/campaign.md)",
      "Edit(/{{CAMPAIGN_PATH}}/CLAUDE.md)",
      "Edit(/{{CAMPAIGN_PATH}}/.claude/**)",
      "Edit(/{{CAMPAIGN_PATH}}/.gitignore)",
      "Write(/{{CAMPAIGN_PATH}}/npcs/**)",
      "Write(/{{CAMPAIGN_PATH}}/locations/**)",
      "Write(/{{CAMPAIGN_PATH}}/factions/**)",
      "Write(/{{CAMPAIGN_PATH}}/items/**)",
      "Write(/{{CAMPAIGN_PATH}}/threads/**)",
      "Write(/{{CAMPAIGN_PATH}}/consequences/**)",
      "Write(/{{CAMPAIGN_PATH}}/beats/**)",
      "Write(/{{CAMPAIGN_PATH}}/adventures/**)",
      "Write(/{{CAMPAIGN_PATH}}/sessions/**)",
      "Write(/{{CAMPAIGN_PATH}}/secrets/**)",
      "Write(/{{CAMPAIGN_PATH}}/.ttrpg-staging/**)",
      "Write(/{{CAMPAIGN_PATH}}/campaign.md)",
      "Write(/{{CAMPAIGN_PATH}}/CLAUDE.md)",
      "Write(/{{CAMPAIGN_PATH}}/.claude/**)",
      "Write(/{{CAMPAIGN_PATH}}/.gitignore)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/npcs/**)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/locations/**)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/factions/**)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/items/**)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/threads/**)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/consequences/**)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/beats/**)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/adventures/**)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/sessions/**)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/secrets/**)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/campaign.md)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/CLAUDE.md)",
      "MultiEdit(/{{CAMPAIGN_PATH}}/.claude/**)",
      "Read(${CLAUDE_PLUGIN_ROOT}/**)",
      "Bash(git status)",
      "Bash(git diff:*)",
      "Bash(git log:*)"
    ]
  }
}
