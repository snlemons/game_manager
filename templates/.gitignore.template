# Workflow staging directory used by ttrpg-gm skills (`/ingest`, `/prep-session`,
# `/wrap-session`) to present diff-style review surfaces in your IDE. The skills
# write proposed content here, the GM edits in place, then on approval the
# content is moved to its final location and the staging directory is removed.
# Should never be committed — it's scratch state across slash-command runs.
.ttrpg-staging/

# Machine-local Claude Code permission rules (absolute paths baked in by the
# scaffolder for this campaign's location on this machine). Regenerated by
# /ingest Phase 1 against a fresh clone.
.claude/settings.json
