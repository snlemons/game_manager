---
name: prep-session
description: Create a new session directory and seed a structured pre-session brief from the campaign's current state. Stub in slice 1 of v0.1 — not yet implemented.
---

# /prep-session

**Status: not yet implemented.**

This slash command will, in a future slice, create a new session directory at `sessions/YYYY-MM-DD-session-N/` and seed a `brief.md` from the campaign's current state (last session's `log.md`, active adventures, open threads, pending beats, recent consequences, relevant NPCs and locations).

Respond to the GM with exactly:

> `/prep-session` is not yet implemented. It will land in a later slice of `ttrpg-gm` v0.1. See the repo's issues for status.

Do not create any files. Do not modify the campaign repo. Do not attempt to run the workflow.
